import React from 'react';
import { ActivityIndicator } from 'react-native';

const ActivityIndicatorUI = ({}) => {
    return (
        <ActivityIndicator
            size="large"
            color="#0000ff"
        />
    )
};

export { ActivityIndicatorUI }